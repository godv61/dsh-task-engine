# 0.23.0 测试执行记录

日期：2026-09-15。Windows，Node 22.22.3，npm 10.9.8；插件基线 7f13fc6 加本次改动；Harness 源码 d347e70390（0.1.3-alpha.1），web profile，Codex 内置 Chromium 浏览器。

## 执行命令

`npm run build`、`npm run typecheck`、`npm test` 均通过。为生成逐条记录，执行 `$env:DSH_TEST_DETAILS='1'; node .p0-test.mjs` 以及 `node --test .resource-test.mjs`；以下表由这次输出生成。

`npm run verify:package` 通过：npm pack、解包、8 个必备文件、排除源码与构建脚本、干净项目 npm 安装、主入口导入、客户端 ModuleLoader 注册、包内 P0 与资源测试、CLI 语法，共 18 个检查点。包内测试与源码测试重复覆盖，不另计为独立用例。该脚本的 CLI 检查仅检查语法，不能替代实际 CLI 启用流程。

## 实机记录

| 编号 | 操作/测试数据 | 实际结果 | 状态 |
|---|---|---|---|
| UI-01 | 最终 tarball 经 dsh plugin --profile web add 安装并重启 3080 | 工作台加载，主机注册工作区读取成功 | 通过 |
| UI-02 | 安装 codex-ui-review-skill 目录 | 系统 filechooser 接收目录，预览 2 文件/162 B，正文不含 frontmatter | 通过 |
| UI-03 | 预览后、确认前检查目标 | Test-Path 为 False；预览没有落盘 | 通过 |
| UI-04 | 确认项目技能安装 | 成功提示与列表更新，附件 SHA-256 与源相同 | 通过 |
| UI-05 | 重复选择同名技能 | 冲突提示出现，确认安装按钮 disabled | 通过 |
| UI-06 | 安装 codex-ui-review-rule.md，切换个人范围 | 预览目标切换为个人规则目录，1 文件/26 B，确认后落盘并显示个人来源 | 通过 |
| UI-07 | 搜索测试技能，打开删除框后选择保留 | 匹配结果正确，目标/永久删除说明可见，取消未删除 | 通过（本次早期同版构建） |
| UI-08 | 安装弹窗按 Escape | 弹窗关闭，返回工作台 | 通过 |
| UI-09 | 台账读取既有任务，选择高风险 | 显示任务验证/审核结果；无匹配时显示调整筛选提示 | 通过 |
| UI-10 | 420×850 视口查看技能页 | 卡片单列、标签与工作区选择换行、按钮可见，随后恢复视口 | 通过 |
| UI-11 | 流程配置页读取 | 显示有效标准预设与绑定，不改写用户配置 | 通过 |

## 自动化明细

P0 是 125 条验收断言，包含部分历史源码文本断言，不等同于 125 个独立端到端场景。资源集合包含 18 个 Node test 用例。P0 没有每条耗时记录；未伪造时间。

| 用例标识 | 状态 | 耗时 | 失败/错误原因 | 证据 |
|---|---|---:|---|---|
| P0-1：standard satisfies high-risk capabilities | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-2：agile lacks a high-risk capability | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-3：minimal lacks a high-risk capability | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-4：preset carries a version | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-5：unknown flow returns UNKNOWN_FLOW | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-6：known flow list is surfaced | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-7：emptying a stage override keeps the core rule | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-8：newTask stores the frozen snapshot | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-9：newTask starts at the frozen start stage | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-10：high_risk on minimal is rejected (got: high_risk task cannot run on flow "minimal" — it lacks the required capabilities (verification_gate, file_scope, review_gate). Use the standard flow or lower the task risk.) | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-11：standard create starts at 需求评审 | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-12：status re-reads the frozen snapshot | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-13：gates come from the frozen standard flow, not the live minimal flow | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-14：create on an unknown flow fails closed (got: invalid .dsh/eng.json — fix the project config first: | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-15：eng.json without a flow field is a config error (got: invalid .dsh/eng.json — fix the project config first: | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-16：inspect on empty project prompts a scan | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-17：propose previews without writing | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-18：propose writes nothing | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-19：apply writes the file | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-20：apply persisted the draft | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-21：propose returns the existing-file hash when the file exists | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-22：apply without existing_hash on an existing file is rejected (got: init apply requires existing_hash returned by propose when AGENTS.md exists — re-propose to obtain it) | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-23：apply with a stale existing_hash is rejected (TOCTOU) (got: AGENTS.md changed since propose (existing_hash mismatch) — re-inspect and re-propose before overwriting) | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-24：apply without overwrite on an existing file is rejected (got: AGENTS.md already exists (2 lines) and is protected. Inspect via init phase=inspect, merge, then resubmit with phase=apply plus overwrite: true (human approval).) | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-25：overwrite requires an approval service (got: overwriting the protected AGENTS.md requires human approval, but the approval service is unavailable) | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-26：apply without expected_hash is rejected (got: init apply requires expected_hash returned by propose — re-propose to obtain it) | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-27：bundled security-redlines exists as expected | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-28：security-redlines is disclosed at the start stage | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-29：a project shadow of a core rule is ignored | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-30：the bundled core rule body wins over a shadow | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-31：inspect reads the session workspace AGENTS.md | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-32：inspect does not read the harness repo AGENTS.md | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-33：task record lands in the session workspace | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-34：task record does not land in the backend base | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-35：cross-stage duplicate artifact id is rejected | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-36：recording an artifact at a non-owning stage is rejected (got: artifact "design" belongs to stage "设计", but the task is at "需求评审" — record it at its owning stage) | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-37：high_risk with text-only evidence is blocked (no command receipt) | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-38：high_risk with an exit-0 receipt passes the verified gate | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-39：high_risk with a non-zero exit receipt is blocked | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-40：high_risk with a timed-out receipt is blocked | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-41：high_risk with an aborted receipt is blocked | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-42：standard summary yields its task id | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-43：item-label summary yields its task id | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-44：non-matching summary yields no task id | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-45：pattern-less flow yields no task id | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-46：task record and eng.json do not violate file scope | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-47：an out-of-scope code file is still flagged | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-48：hook disables git octal path quoting | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-49：hook reads NUL-separated raw-byte paths | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-50：hook includes deletions and type changes in the scope check | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-51：hook checks the task frozen flow snapshot | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-52：hook extracts the task id independent of the live config | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-53：hook bundles the single source (workflows.ts), not a hand mirror | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-54：workbench writeInit refuses to overwrite without an explicit intent | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-55：writeInit request carries an overwrite field | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-56：node marker resolves to node | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-57：root discovery climbs to the package.json directory | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-58：pom.xml resolves to java | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-59：go.mod resolves to go | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-60：Cargo.toml resolves to rust | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-61：requirements.txt resolves to python | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-62：empty tree reports unknown type | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-63：root discovery falls back to the start directory | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-64：mixed tree reports unknown, never a silent single-language guess | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-65：root discovery stops at the git repository | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-66：node default verify command | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-67：java default verify command | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-68：go default verify command | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-69：unknown type has no default command | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-70：governance files are recognized as present | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-71：absent governance files are not listed | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-72：init write escaping the root is rejected (got: write target D:\outside.txt escapes the project root D:\repo) | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-73：absolute target outside the root is rejected (got: write target C:\elsewhere\x.txt escapes the project root D:\repo) | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-74：hook bundles the risk policy table (single source) | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-75：hook requires high_risk before touching sensitive paths | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-76：project rules list from .dsh/rules/*.md | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-77：project skills list only dirs carrying SKILL.md | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-78：hashConfig is a deterministic sha256 | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-79：editing the frozen config changes the hash | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-80：status on a tampered snapshot is rejected (got: task "HASH-1" snapshot hash mismatch — the frozen workflow config was edited after creation. Restore the original record or recreate the task; the gate refuses to run on a tampered snapshot.) | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-81：hook reuses the single hashConfig source | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-82：hook bundles the sha256 snapshot check | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-83：verify_hook reports an absent hook | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-84：verify_hook passes on the bundled hook | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-85：verify_hook rejects a tampered hook (got: hook integrity FAILED — .git/hooks/commit-msg differs from the bundled gate; reinstall with install_hook) | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-86：downgrade without an approval service is rejected (got: downgrading a high_risk task requires human approval, but the approval service is unavailable) | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-87：same-risk set_risk is a no-op | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-88：approved downgrade succeeds | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-89：downgrade is recorded in the audit trail | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-90：fresh task shows no bindings drift | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-91：create records the bundled-rules fingerprint | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-92：verify falls back to the eng.json command and fails loud without a shell service (got: verify 带 command 需要 host 提供 shell 服务（用于跑真实验证命令），但当前未挂载) | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-93：verify falls back to the language default (node → npm test) and fails loud without a shell service (got: verify 带 command 需要 host 提供 shell 服务（用于跑真实验证命令），但当前未挂载) | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-94：unknown-type verify keeps the legacy self-reported path | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-95：two tasks in one repo advance independently | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-96：propose returns the content hash | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-97：apply with a mismatched hash is rejected (got: init content changed since propose (expected_hash mismatch) — resubmit the identical proposed content) | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-98：apply with the matching hash writes the file | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-99：project binding dir (.dsh/rules, .dsh/skills) stays committable by normal tasks | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-100：env files remain sensitive | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-101：dev_task writes carry the sandbox mode (default workspace-write) | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-102：dev_task writes carry the session workspace root so in-workspace paths are never misjudged as out-of-root | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-103：escalation without a justification is rejected (got: invalid escalation: sandbox_permissions requires a justification) | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-104：approved escalation upgrades the per-call write mode | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-105：escalation without an approval service fails closed (got: sandbox escalation to danger-full-access requires human approval, but the approval service is unavailable) | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-106：raising risk on a minimal flow is rejected (same gate as create) (got: cannot raise risk to high_risk on flow "minimal" — it lacks the required capabilities (verification_gate, file_scope, review_gate). Use the standard flow.) | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-107：raising risk on the standard flow succeeds | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-108：remote rejects non-absolute workspace paths | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-109：remote rejects system-wide roots | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-110：bare verify ran the default command | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-111：verify ran in the recorded project root, not the session cwd | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-112：receipt binds the recorded project root | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-113：revision increments on every write | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-114：concurrent-write CAS guard is present | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-115：absolute workspace passes | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-116：trailing slash is normalized | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-117：relative path is rejected (got: task-engine: workspace path must be absolute: relative/path) | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-118：system root is rejected (got: task-engine: path is not an allowed workspace: C:/Windows/system32) | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-119：case variant of a system root is rejected (got: task-engine: path is not an allowed workspace: c:/windows) | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-120：registered workspace passes under any case | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-121：unregistered workspace is rejected once the registry is active (got: task-engine: workspace not registered on this host: D:/other-project) | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-122：strict mode keeps rejecting unregistered workspaces (got: task-engine: workspace not registered on this host: D:/still-unregistered) | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-123：strict mode still admits registered workspaces | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-124：CAS rejects writes when version changes after the content read (got: task "CAS-RACE" changed concurrently (file version moved) — reload status and retry this operation) | 通过 | 未单独计时 | 无 | P0 明细输出 |
| P0-125：CAS preserves the concurrent writer record | 通过 | 未单独计时 | 无 | P0 明细输出 |
| Import-1：preview has no filesystem side effects and reports exact bytes | 通过 | 11.0717 ms | 无 | Node TAP 输出 |
| Import-2：confirmed install preserves binary assets and refuses duplicates | 通过 | 8.0443 ms | 无 | Node TAP 输出 |
| Import-3：changed content and changed installation target invalidate preview | 通过 | 2.3299 ms | 无 | Node TAP 输出 |
| Import-4：path traversal, Windows ADS and reserved names are rejected | 通过 | 1.572 ms | 无 | Node TAP 输出 |
| Import-5：case-colliding filenames are rejected | 通过 | 1.3899 ms | 无 | Node TAP 输出 |
| Import-6：bundled names and ambiguous containers are rejected | 通过 | 0.8907 ms | 无 | Node TAP 输出 |
| Import-7：single-file rule accepts UTF-8 BOM and CRLF; empty rules are refused | 通过 | 3.1442 ms | 无 | Node TAP 输出 |
| Import-8：file and directory prefix conflicts fail during preview | 通过 | 0.7124 ms | 无 | Node TAP 输出 |
| Import-9：failed installation removes only its own incomplete directory | 通过 | 6.7741 ms | 无 | Node TAP 输出 |
| Import-10：host directory scanner resolves one container child and excludes caches | 通过 | 5.6005 ms | 无 | Node TAP 输出 |
| Import-11：host scanner rejects symlink or junction source paths | 通过 | 2.4246 ms | 无 | Node TAP 输出 |
| Import-12：deep directories and oversized files fail before installation | 通过 | 21.9509 ms | 无 | Node TAP 输出 |
| Import-13：invalid UTF-8, encoding and absent source directories report errors | 通过 | 1.2419 ms | 无 | Node TAP 输出 |
| Import-14：registered host workspace wins over client spelling; unknown paths fail | 通过 | 2.6087 ms | 无 | Node TAP 输出 |
| Import-15：legacy host retains its explicit workspace policy | 通过 | 0.214 ms | 无 | Node TAP 输出 |
| Import-16：host registry failures are not downgraded to legacy permissions | 通过 | 0.1672 ms | 无 | Node TAP 输出 |
| Import-17：ledger shows absent storage as empty and propagates access errors | 通过 | 18.696 ms | 无 | Node TAP 输出 |
| Import-18：ledger rejects malformed records instead of pretending there are no tasks | 通过 | 0.3443 ms | 无 | Node TAP 输出 |

## 缺陷与复测

首次大文件边界测试触发 Base64 正则回溯导致 RangeError；已改用 Buffer 解码后规范编码比对，本次 Import-12 通过。目录中同时含文件 a 与 a/b 的输入现于预览拒绝（Import-8）；磁盘故障回滚通过独立写入故障注入覆盖（Import-9），且验证兄弟文件保留。任务 CAS 测试在读取期间注入另一写者，拒绝覆盖并保留并发内容。

## 数据与环境

单元测试使用 mkdtemp 独立临时目录并在 teardown 删除；文件写入故障注入在 finally 恢复 fs 方法。实机只添加 codex-ui-review-skill 与 codex-ui-review-rule.md 两个一次性测试资源，清理结果见报告。用户原有 software-testing 资源和任务未修改。

profile 的 pnpm peers check 报缺少五个 Harness peer；实际运行由 Harness 源码环境提供，工作台功能通过。干净 npm 项目自动解析 peer 安装通过。显式添加已实测 0.1.3-alpha.1 预发布分支，避免 semver 将其排除。没有通过关闭依赖安全策略来安装。

## 首轮 CI 差异与修复

GitHub run 34950017436：Node 22 与 24 的安装、类型检查和 npm test 均通过；Node 24 发布包脚本错误地把测试成功输出判为失败，Node 22 发布包任务随后被矩阵 fail-fast 取消。原因是检查脚本依赖 `# fail 0`，却没有固定 Node test reporter，Node 24 默认输出格式与本机不同。修复为显式 `--test-reporter=tap`；矩阵关闭 fail-fast 以保留各版本完整结果，没有跳过测试或降低通过条件。复验状态见 GitHub Actions。
